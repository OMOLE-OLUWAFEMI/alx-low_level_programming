#include "main.h"
/**
 * print_alphabet - print the alphabet
 *
 * Return: Always success
 */
vold print_alphabet(void)
{

	int n;

	for (n = 'a'; n <= 'z'; n++)
	{
		putchar(n);
	}
	{
		putchar('\n');
	}
